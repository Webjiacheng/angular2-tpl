import { Component } from '@angular/core';

@Component({
  selector: 'app-root', //定义一个自定义标签，在html中对应为<app-root></app-root>
  templateUrl: './app.component.html' //给这个自定义组件指定的html模板代码路径
})
export class AppComponent { }
